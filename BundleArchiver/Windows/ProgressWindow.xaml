<Window x:Class="BundleArchiver.Windows.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BundleArchiver.Windows"
        xmlns:mvvm="clr-namespace:BundleArchiver.ViewModels"
        xmlns:conv="clr-namespace:BundleArchiver.Converters"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Zipping Files" Height="175" Width="600">

    <Window.DataContext>
        <mvvm:ProgressViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <conv:ProgressToTextConverter x:Key="ProgressConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="{Binding Status}" Margin="0,0,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="0.5*"/>
                </Grid.ColumnDefinitions>

                <ProgressBar Minimum="0"
                             Maximum="100"
                             Height="15"
                             Value="{Binding Progress}"/>
                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Text="{Binding Progress, Converter={StaticResource ProgressConverter}}"/>
            </Grid>
        </StackPanel>

        <StackPanel Margin="5" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Width="80"
                    Margin="5"
                    Command="{Binding ContinueCommand}"
                    Content="OK"
                    />
            <Button Width="80"
                    Margin="5"
                    Command="{Binding CancelCommand}"
                    Content="Cancel"
                    />
        </StackPanel>

    </Grid>
</Window>
